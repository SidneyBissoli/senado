% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/senators.R
\name{sen_senator_speeches}
\alias{sen_senator_speeches}
\title{Speeches of a senator}
\usage{
sen_senator_speeches(code, start_date = NULL, end_date = NULL)
}
\arguments{
\item{code}{Senator code, as in the \code{senator_code} column of
\code{\link[=sen_senators]{sen_senators()}}.}

\item{start_date, end_date}{Period, as \code{Date} or \code{"YYYY-MM-DD"}. With
neither, the API returns the last 30 days. With only \code{start_date}, the
period runs until today.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per speech:
\describe{
\item{senator_code}{Senator code (integer).}
\item{speech_code}{Speech code (integer).}
\item{date}{Date of the speech (Date).}
\item{speech_type, speech_type_description}{Kind of floor use (e.g.
\code{"DIS"}, "Discurso").}
\item{party, state}{Party and state of the senator \strong{on the date of
the speech}.}
\item{house}{\code{"SF"} (Senate) or \code{"CN"} (National Congress).}
\item{summary}{Summary of the speech, in Portuguese.}
\item{keywords}{Indexing terms, comma-separated.}
\item{text_url, file_url}{Links to the full text (web page and file).}
\item{session_id, session_type, session_number, session_date}{The
plenary session in which the speech was given. \code{session_id} is the
same key used by \code{sen_votes()} and \code{sen_sessions()}.}
\item{interjections}{List-column: senators who interjected
(\code{senator_code}, \code{name}).}
\item{publications}{List-column: where the speech was published
(\code{source}, \code{date}, \code{first_page}, \code{last_page}, \code{republication},
\code{url}).}
}
A period without speeches yields zero rows; an unknown \code{code} raises an
error of class \code{senado_error_not_found}.
}
\description{
Returns the floor speeches of a senator in a period, with a summary,
keywords and links to the text. The text itself is not part of the
response.
}
\details{
The API silently truncates any window longer than one year, keeping only
the year before \code{end_date}. To return what was asked for, the function
splits longer periods into calendar years, makes one request per year
(with a progress bar) and stacks the results.
}
\examples{
\dontshow{if (senado:::sen_api_available()) withAutoprint(\{ # examplesIf}
# Last 30 days
sen_senator_speeches(825)

# One month
sen_senator_speeches(825, start_date = "2024-05-01", end_date = "2024-05-31")
\dontshow{\}) # examplesIf}
}
\seealso{
Other senators:
\code{\link[=sen_senator]{sen_senator()}},
\code{\link[=sen_senator_committees]{sen_senator_committees()}},
\code{\link[=sen_senator_mandates]{sen_senator_mandates()}},
\code{\link[=sen_senators]{sen_senators()}}
}
\concept{senators}
