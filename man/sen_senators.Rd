% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/senators.R
\name{sen_senators}
\alias{sen_senators}
\title{List senators}
\usage{
sen_senators(
  legislature = NULL,
  state = NULL,
  party = NULL,
  status = c("all", "holder", "substitute"),
  in_office = NULL
)
}
\arguments{
\item{legislature}{\code{NULL} (default) for the senators currently in office;
a legislature number (e.g. \code{57}) for everyone with a mandate in that
legislature; or two numbers (e.g. \code{c(55, 57)}) for a range. See
\code{\link[=sen_legislatures]{sen_legislatures()}}.}

\item{state}{Two-letter state abbreviation (e.g. \code{"SP"}). Filtered by the
API.}

\item{party}{Party abbreviation (e.g. \code{"PT"}). \strong{Only for the senators
currently in office} (\code{legislature = NULL}); see the section on party
below.}

\item{status}{\code{"all"} (default), \code{"holder"} for elected holders of the
seat or \code{"substitute"} for substitutes (\emph{suplentes}). Filtered by the
API.}

\item{in_office}{Only with \code{legislature}: \code{TRUE} for those who actually
took office during the legislature, \code{FALSE} for those who did not,
\code{NULL} (default) for both.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per senator:
\describe{
\item{senator_code}{Senator code (integer). The join key across the
package.}
\item{name, full_name}{Parliamentary name and full civil name.}
\item{sex}{\code{"Masculino"} or \code{"Feminino"}.}
\item{treatment}{Form of address (\code{"Senador"}, \code{"Senadora"}).}
\item{party}{Party abbreviation. See the section on party below.}
\item{state}{State of the mandate.}
\item{participation}{\code{"Titular"}, \code{"1º Suplente"} or
\code{"2º Suplente"}.}
\item{mandate_code}{Mandate code (integer).}
\item{first_legislature, second_legislature}{The two legislatures an
eight-year mandate spans (integer).}
\item{bloc}{Parliamentary bloc. Current senators only.}
\item{board_member, leadership_member}{Whether the senator sits on the
Senate board or holds a leadership post (logical). Current senators
only.}
\item{email, photo_url, page_url}{Contact and web addresses.}
\item{mandates}{List-column: a tibble with every mandate the senator
held in the requested legislatures (\code{mandate_code}, \code{state},
\code{participation}, \code{first_legislature}, \code{second_legislature}). The
flat mandate columns above describe the most recent one.}
}
With no match, a tibble with zero rows and the same columns.
}
\description{
Returns the senators currently in office or, with \code{legislature}, everyone
who held a mandate in one legislature or in a range of legislatures --
holders and substitutes, whether or not they ever took office.
}
\section{Party in historical lists}{

Only the list of current senators carries a reliable party. In the lists
by legislature the API fills \code{party} for some senators only (153 of the
245 in the 57th legislature), and with the party on record today, not
the party at the time. That is why the \code{party} argument is refused
together with \code{legislature}. For the party in each period use
\code{\link[=sen_senator_mandates]{sen_senator_mandates()}}.
}

\examples{
\dontshow{if (senado:::sen_api_available()) withAutoprint(\{ # examplesIf}
# Senators currently in office
sen_senators()

# From one state, from one party
sen_senators(state = "SP")
sen_senators(party = "PT")

# Seats in office by state
table(sen_senators()$state)

# Everyone who took office during the 56th legislature
sen_senators(legislature = 56, in_office = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
Other senators:
\code{\link[=sen_senator]{sen_senator()}},
\code{\link[=sen_senator_committees]{sen_senator_committees()}},
\code{\link[=sen_senator_mandates]{sen_senator_mandates()}},
\code{\link[=sen_senator_speeches]{sen_senator_speeches()}}
}
\concept{senators}
