% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/senators.R
\name{sen_senator_committees}
\alias{sen_senator_committees}
\title{Committee memberships of a senator}
\usage{
sen_senator_committees(code, active = NULL, committee = NULL)
}
\arguments{
\item{code}{Senator code, as in the \code{senator_code} column of
\code{\link[=sen_senators]{sen_senators()}}.}

\item{active}{\code{TRUE} for current memberships only, \code{FALSE} for past
memberships only, \code{NULL} (default) for both. Filtered by the API.}

\item{committee}{Committee abbreviation (e.g. \code{"CCJ"}) to keep only the
memberships in that committee. Filtered by the API.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per membership:
\describe{
\item{senator_code}{Senator code (integer).}
\item{committee_code}{Committee code (integer).}
\item{committee, committee_name}{Abbreviation and name.}
\item{house}{\code{"SF"} (Senate) or \code{"CN"} (National Congress).}
\item{participation}{\code{"Titular"} or \code{"Suplente"}.}
\item{start_date, end_date}{Membership period (Date); \code{end_date} is
\code{NA} for current memberships.}
}
A senator with no membership yields zero rows; an unknown \code{code} raises
an error of class \code{senado_error_not_found}.
}
\description{
Returns the committees a senator sits or has sat on. Besides Senate
committees, the list includes joint committees of the National Congress,
parliamentary groups and fronts.
}
\examples{
\dontshow{if (senado:::sen_api_available()) withAutoprint(\{ # examplesIf}
sen_senator_committees(5322)

# Current memberships only
sen_senator_committees(5322, active = TRUE)
\dontshow{\}) # examplesIf}
}
\seealso{
Other senators:
\code{\link[=sen_senator]{sen_senator()}},
\code{\link[=sen_senator_mandates]{sen_senator_mandates()}},
\code{\link[=sen_senator_speeches]{sen_senator_speeches()}},
\code{\link[=sen_senators]{sen_senators()}}
}
\concept{senators}
