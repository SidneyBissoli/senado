% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/senators.R
\name{sen_senator_mandates}
\alias{sen_senator_mandates}
\title{Mandates of a senator}
\usage{
sen_senator_mandates(code)
}
\arguments{
\item{code}{Senator code, as in the \code{senator_code} column of
\code{\link[=sen_senators]{sen_senators()}}.}
}
\value{
A \link[tibble:tibble]{tibble::tibble} with one row per mandate:
\describe{
\item{senator_code}{Senator code (integer).}
\item{mandate_code}{Mandate code (integer).}
\item{state}{State of the mandate.}
\item{participation}{\code{"Titular"}, \code{"1º Suplente"} or
\code{"2º Suplente"}.}
\item{first_legislature, first_start_date, first_end_date}{First
legislature of the mandate and its dates.}
\item{second_legislature, second_start_date, second_end_date}{Second
legislature of the mandate and its dates.}
\item{holder_code, holder_name}{When the senator is a substitute: the
holder of the seat. \code{NA} otherwise.}
\item{substitutes}{List-column: \code{participation}, \code{senator_code},
\code{name}.}
\item{terms}{List-column, one row per period in office: \code{term_code},
\code{start_date}, \code{end_date}, \code{leave_code}, \code{leave_description},
\code{reading_date}. The last four are \code{NA} while the term is ongoing.}
\item{parties}{List-column, one row per party affiliation during the
mandate: \code{party_code}, \code{party}, \code{party_name}, \code{affiliation_date},
\code{disaffiliation_date}.}
}
An unknown \code{code} raises an error of class \code{senado_error_not_found}.
}
\description{
Returns every mandate a senator has held, with the substitutes, the
periods actually in office and the party affiliations of each mandate.
}
\details{
Each mandate carries three lists of different things, so they are kept as
list-columns rather than multiplied into rows. Open one at a time:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{mandates <- sen_senator_mandates(5322)
tidyr::unnest(
  mandates[c("mandate_code", "parties")], parties
)
}\if{html}{\out{</div>}}

A party that changed its name appears under the current abbreviation.
}
\examples{
\dontshow{if (senado:::sen_api_available()) withAutoprint(\{ # examplesIf}
mandates <- sen_senator_mandates(5322)
mandates

# Party affiliations during the most recent mandate
mandates$parties[[1]]
\dontshow{\}) # examplesIf}
}
\seealso{
Other senators:
\code{\link[=sen_senator]{sen_senator()}},
\code{\link[=sen_senator_committees]{sen_senator_committees()}},
\code{\link[=sen_senator_speeches]{sen_senator_speeches()}},
\code{\link[=sen_senators]{sen_senators()}}
}
\concept{senators}
